============
Installation
============

At the command line::

    $ easy_install django-dropbox-upload-handler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dropbox-upload-handler
    $ pip install django-dropbox-upload-handler
